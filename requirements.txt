# Python (pip) dependencies for this project.
#
# This is a ROS 2 workspace, not a standalone Python package: the ROS 2
# interfaces (rclpy, sensor_msgs, geometry_msgs, std_msgs, cv_bridge, ...)
# and the simulators (classic Gazebo / gz sim) are installed via apt and
# the ROS 2 package manager, declared in each package's package.xml:
#   - depth_project/depth_project/package.xml
#   - my_yolo_world/my_yolo_world/package.xml
#   - yolo_nav/yolo_nav/package.xml
# See the README "Environment" section for the full system-level setup
# (ROS 2 distro, Gazebo version, TurtleBot3 packages).
#
# The packages below are the plain pip-installable Python libraries the
# nodes and training script import directly and are NOT covered by
# package.xml / rosdep.

torch>=2.1
torchvision>=0.16
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
ultralytics>=8.0
